1. 프로젝트 개요:

이 프로그램은 SIC/XE머신 구현을 위한 전 단계로서 기본적인 shell명령어들과, object 코드가 적재되고 실행될 메모리 공간, mnemonic(ADD, COMP, FLOAT 등)을 opcode값으로 변환하는 OPCODE 테이블과 관련 명령어들을 구현한 프로그램이다.



2. 컴파일 방법:

Makefile, 필요한 Source코드(20161595.h, 20161595.c) 및 파일(opcode.txt)이 모두 들어있는 폴더에서 'make'라는 명령어를 통해 컴파일 한다.



3. 실행 방법:

컴파일이 끝난 후 20161595.out 파일이 생성되고 ./20161595.out을 통하여 프로그램을 실행한다.
