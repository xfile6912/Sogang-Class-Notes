1. 컴파일 방법:
Makefile, 필요한 Source코드(myshell.h, myshell.c, csapp.h, csapp.c)이 모두 들어있는 각 Phase폴더에서 'make'라는 명령어를 통해 컴파일 한다.

2. 실행 방법:
컴파일이 끝난 후 myshell 실행 파일이 생성되고 ./myshell을 통하여 프로그램을 실행한다.


Phase2
부모 프로세스가 파이프 라인의 각 명령에 대하여 pipe와 새로운 자식 프로세스를 만들고 pipe를 통하여
파이프 앞쪽의 명령을 담당하는 자식 프로세스의 출력을 파이프 뒤쪽의 명령을 담당하는 자식프로세스의 입력으로 들어가도록 
연결시켜 줌으로써 리눅스의 파이프라인 구조를 수행할 수 있는 Shell Program이다.