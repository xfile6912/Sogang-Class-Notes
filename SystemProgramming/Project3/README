





1. 컴파일 방법:

Makefile, 필요한 Source코드(20161595.h, 20161595.c) 및 파일(opcode.txt, proga.obj, progb.obj, progc.obj, copy.obj)이 모두 들어있는 폴더에서 'make'라는 명령어를 통해 컴파일 한다.



2. 실행 방법:

컴파일이 끝난 후 20161595.out 파일이 생성되고 ./20161595.out을 통하여 프로그램을 실행한다.
